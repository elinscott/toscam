# This config will work for HECTOR as long as the following modules
# have been loaded:

# module load PrgEnv/pathscale
# module load fftw/3.1.1

# Adding the command
# export XTPE_INFO_MESSAGE_OFF=T 
# to .bashrc is also highly recommended, to reduce the spam produced by ftn

F90 = ftn
FFLAGS = -DFFTW3 -DMPI
LIBS = 
DEBUGFLAGS = -trapuv -g -ffortran-bounds-check -C
OPTFLAGS = -O2
