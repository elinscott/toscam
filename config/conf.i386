#
# ONES configuration file
#
# For i386 compatible running linux
#

F90 = pgf90
FFLAGS = -DFFTW -Msecond_underscore -Mdalign
LIBS = -lmkl_lapack -lmkl_p4 -lpthread -lrfftw -lfftw
OPTFLAGS = -O -tp p6 -fast
DEBUGFLAGS = -g -Mbounds -Mdclchk
