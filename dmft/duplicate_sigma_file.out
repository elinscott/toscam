#!/bin/bash

MYSIGMA=$1
LIMIT=$2
SPECIES=$3
SPIN=$4

for ((a=1; a <= LIMIT ; a++))  # Double parentheses, and "LIMIT" with no "$".
do
if [ $a != $1 ]
then
 echo "Copy $1 to $a"
 j="sigma_output"$a"_"${SPECIES}"_"${SPIN}
 echo "target = $j"
 cp sigma_output$1_${SPECIES}_${SPIN} $j
fi
done 


